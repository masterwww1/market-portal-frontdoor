# B2Bmarket Frontend Environment Variables

# Authentication API URL (Lambda or FastAPI backend)
# For Lambda: https://your-api-gateway-url.execute-api.region.amazonaws.com/stage
# For FastAPI backend: /api/auth (relative)
VITE_AUTH_API_URL=/api/auth
